server:
  port: 9527

spring:
  application:
    name: crush-gateway
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml

management:
  endpoints:
    web:
      exposure:
        include: '*'